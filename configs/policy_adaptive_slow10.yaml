# Tuned for slow_10kbps adaptive runs: reduce outbox pressure and protect AoI.
arms:
  - { tau: 1.5, kbits: 6 }
  - { tau: 3.0, kbits: 8 }
  - { tau: 6.0, kbits: 10 }
reward:
  alpha: 1.3
  beta: 1.0
  gamma: 0.4
safety:
  aoi_max_ms: 2500
  mae_max: 2.0
  safety_force_emit_on_aoi: true
diagnostics:
  enabled: false
